# CLAUDE.md ·《系统是被打出来的》系统设计实战手册

## 项目定位

一本 HTML 电子书。核心主张：**系统不是设计出来的，是被流量打出来的。**
本书解决的痛点：工程师在真实工作中没有机会实战大流量系统、没有机会处理极端情况。
本书是系统设计的"飞行模拟器"——生产环境不给的练习机会，这里管够。

主线：虚构抢票平台「秒抢」从 0 到亿级用户的挨打史。读者是主角工程师，全书以第二人称推进。

## 产品形态（系列硬约定）

- 纯静态 HTML，无构建依赖，可直接 GitHub Pages 部署
- **深色模式为默认且必须完美**（作者常在夜间阅读，这是硬性要求）
- 中文
- 目录结构：
  ```
  index.html            目录 + 导读（如何使用本书）
  ch01.html … ch16.html 正文 16 章
  level1.html … level4.html 四个实战关卡
  appendix-cards.html   附录A 病历卡图鉴
  appendix-interview.html 附录B 面试映射总表
  assets/style.css      全站样式（从样章抽取）
  assets/sim-*.js       各模拟器脚本（每个模拟器独立文件）
  tools/                校验脚本（不部署）
  ```
- 每页顶部 book-bar + 章节导航（上一章/目录/下一章），样式沿用样章

## 设计令牌（以样章 sample/ch05-cache-brothers.html 为唯一视觉基准）

样章的 `:root` CSS 变量、字体栈（serif 标题 / sans 正文 / mono 运维元素）、
组件样式（.pager .metric .decision .opt .verdict .case-card .sim .timeline .lesson
.interview .quiz）**原样抽取为 assets/style.css**，不得另起炉灶。
新增组件必须复用现有令牌，禁止引入新的裸色值。

## 每章模板（七段式，样章即标准）

1. **值班现场**（05·1 式编号）：第二人称故障/危机开场。给监控现象（.pager 告警 + .metrics 指标卡），
   信息量必须足够读者做判断。随后嵌入**决策点 D1**。
2. **原理**：解释这一章的核心机制。必须有一个"契约级"的核心公式或核心句（如第 5 章的
   `DB 流量 = 总流量 × (1 − 命中率)`），并用具体数字代入演示。
3. **病历卡**：本章分配到的故障模式卡（见 SKELETON.md 分配表）。五行固定结构：
   机理 / 症状 / 易发场景 / 处方 / 案例。症状行必须给出曲线形状符号（如 ▁▂▃ / ▁█▁ / ██▁）
   ——"形状就是诊断"是全书记忆钩子。编号全书唯一、与附录图鉴一致。
4. **模拟器**（仅分配到的章节）：见下方模拟器工程规则。
5. **权衡（决策点 D2）**：trade-off 题。**所有选项都必须是"对的"，每个选项给出账单**
   （代价 + 适用场景），结尾给出「秒抢」的选择及理由。与自测题的本质区别：无标准答案，只有代价。
6. **复盘**：回到开场现场，给出正确处置时间线（.timeline，顺序本身是知识）+ 三条教训（.lesson，壹貳叁编号）。
7. **自测**：4 道选择题（.quiz），必含一道"看症状形状诊断"型和一道数字计算型。
   每题解析不止说对错，要说明**为什么诱人的错误选项是错的**。
8. **面试映射**（.interview）：3 个高频追问 + 一句话答题骨架。骨架给结构不给答案全文。

没有分配模拟器的章节跳过第 4 段，其余段落一律齐全。

## 交互组件约定

- 决策点/自测统一用 `.opt` 按钮 + `.verdict` 揭示块。`data-k`: good / bad / mid（trade-off 全 mid）。
  `data-d="N"` 对应 `#v-dN`，`data-q="qN"` 对应 `#v-qN`。交互脚本全站共用一份。
- 禁止使用 localStorage / sessionStorage / 外部 CDN / 任何网络请求。全部自包含。

## 模拟器工程规则（全书 5–6 个，见 SKELETON.md 分配）

1. 原生 JS + canvas，无依赖；参照 sample 中 Sim-2（缓存雪崩）的结构：
   状态机 tick 模型 + 时间窗折线图 + 指标卡 + 事件日志（带模拟时钟）+ 控件。
2. 每个模拟器配 **🧪 实验清单**（2–3 个按顺序做的实验），每个实验对应一个明确的教学结论。
3. **正文声称的每一个实验现象，必须先用无头模型测试验证**（node 脚本，复制状态机核心逻辑，
   断言现象成立），测试文件放 tools/sim{N}-model-test.js。参考 tools/sim2-avalanche-model-test.js。
   验证不过 = 要么改模型，要么改正文声称，禁止上线与模型不符的文字。
4. 参数默认值必须让"默认剧情"有戏剧性但可恢复；死亡态必须可通过教学动作解出（或明确告知只能重置）。

## 文风

- 值班现场段落：故事化、紧张、具体到分钟和数字；技术原理段落：精确、不煽情。
- 允许比喻，但优先取材于系统世界本身（契约、账单、止血、病历、死亡螺旋）。
- 绝对化表述（"永远""唯一""必然"）出现时必须自检：要么给出成立条件，要么改为限定表述。
- 章间互相引用必须写明章号（"第 10 章会正面解决"），且与骨架一致——校验脚本会查。
- 数字优先给量级和倍数关系，避免容易过时的具体产品参数。

## 构建顺序

1. 抽取 assets/style.css + 共用交互脚本；index.html（含导读：四机制说明 + 阅读路径）
2. 按章号顺序逐章构建（第 5 章直接收编样章，仅改造为引用共享 assets）
3. 每个模拟器：先写无头模型测试 → 验证实验剧本 → 再嵌入页面
4. 四个实战关卡（QUALITY.md 事实核查协议优先级最高）
5. 附录 A / B（编号与映射由脚本从正文派生，禁止手数）
6. 全站横切检查（见 QUALITY.md）后才算完成
