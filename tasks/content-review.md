# 任务书 · 终审内容校验（只读，不改任何文件）

你是**独立终审**。你没有参与这本书的写作，这正是你的价值——请以第一次读到它的读者视角挑毛病。
工作目录 `/Users/kyx/Documents/sd-handbook-build-pack-v1`。

## 先读这四份规格

1. `CLAUDE.md` —— 七段式模板、文风、交互约定
2. `QUALITY.md` —— 质量红线（第一节六条、第二节真实事故核查协议）
3. `BIBLE.md` —— 连续性圣经：结构硬规则、成长刻度表、逐章契约表、病历卡总表
4. `sample/ch05-cache-brothers.html` —— 唯一视觉与内容基准

## 校验对象（23 页）

`index.html`、`ch01.html`–`ch16.html`、`level1.html`–`level4.html`、
`appendix-cards.html`、`appendix-interview.html`

## 重点查这三条——脚本查不了，只能靠通读

1. **每道自测题解析是否真的说明「为什么诱人的错误选项是错的」**，还是只写了「B 对」。
   共 64 题；每一章至少抽查 1 题，可疑的多查。
2. **决策点 D1 的信息量是否足够**——`.pager` 告警 + `.metrics` 指标卡给出的数字，
   够不够读者在不看答案时选出正确项？少给关键数字的章请点名。
3. **决策点 D2 每个选项是否都给了「账单」**（代价 + 适用场景），且三个选项都确实是"对的"
   （不存在一个明显最优解）。

## 另外请独立判断

4. **文风一致性**：值班现场是否故事化、具体到分钟和数字；原理段是否精确不煽情。
   与样章 ch05 相比，**哪几章明显偏弱**？点名。
5. **真实事故事实边界**：`sources/FACTS-level1..4.md` 是逐句核对清单，
   `sources/raw/` 是官方复盘原文留档。抽查 level1–4 页面上带 `[S1]` 角标的陈述，
   是否真能在对应 FACTS 文件找到依据。
   **特别核对：level1 声称「四层失效、第五层活了」（而非坊间的「五层全失效」）——
   请打开 `sources/raw/gitlab-2017-postmortem.txt` 亲自确认这个说法成立。**
6. **教学价值**：作为「系统设计飞行模拟器」，**哪一章最弱、最该重写**？

## 输出

写入 `tasks/REVIEW-verdict.md`，格式：

```
内容校验：通过   （或：不通过）

## Findings（按严重度排序）
1. 文件 · 位置 · 问题 · 建议
...

## 本次校验覆盖了什么、没覆盖什么
...
```

**不要修改任何 .html / assets / tools 文件。** 只写 `tasks/REVIEW-verdict.md` 这一个文件。
不要跑构建脚本（已全绿，不是你的任务）。你的价值在于读出脚本读不出来的东西。
**如果你认为不通过，就写不通过**——请不要为了让人好过而放水。
