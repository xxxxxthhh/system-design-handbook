# Fable 复审报告 · d3b9617

> 任务书要求写到 `tasks/FABLE-RECHECK.md`。**这个名字在本机就是任务书本身**——APFS 大小写不敏感，
> `ls tasks/FABLE-RECHECK.md` 直接命中 `fable-recheck.md`（3227 字节，19:06）。照写会把任务书覆盖掉。
> 因此改用 `FABLE-RECHECK-REPORT.md`，与 `V2-TRY-REPORT.md` / `UI-MEASURE-REPORT.md` 同一约定。
> 只读复审，未改任何文件、未 commit。derive 一致性是在 scratchpad 里拷贝一份仓库跑的，没碰工作区。

## 复审：不通过

三件阻断级的事，任何一件都足以拦下：

1. **D1 槽位置换脚本把 11 章的决策点标题和题目吃掉了。** ch02/04/05/06/08/09/11/12/13/15/16 的
   `<div class="decision" id="d1">` 现在直接跟四个按钮，`<span class="decision-tag">决策点 D1</span>` 与
   `<h3>此刻是 23:11。你的第一个动作是什么？</h3>` 这两行在这 11 章全部消失（ch01/03/07/10/14 因为
   没被置换所以还在）。读者看到的是四个没有题目的按钮。`validate.py` 只检查 d1 存在与 good 数量
   （tools/validate.py:216–230），所以「validate 23/23」没拦住。丢失的 h3 原文可从 `d3b9617^` 直接取回：
   ch02 23:11 / ch04 16:42 / ch05 02:49 / ch06 14:06 / ch08 11:02 / ch09 01:33 / ch11 20:17 /
   ch12「D-7 10:08，你先拍板哪一件事？」/ ch13「此刻是周三 16:40。两周排障仍无结论，你先改变什么？」/
   ch15 14:08 / ch16「10:04，第一次生产注入应该怎么开始？」。
2. **置换后有一处正文引用错位**——正是任务书让我重点找的那种：`ch05.html:186` 模拟器实验清单写
   「也是决策点 D1 里 **C 选项**的数学证明」，指的是网关限流；置换后网关限流是 **A**，C 变成了「紧急给数据库扩容」。
   实验现在在「证明」错误选项。
3. **ch16 四分支的分派规则自相矛盾，Opus 那位读者仍然落不干净**（细节见下）。

其余是「已解决但带伤」或「未完全解决」，逐条在下。

---

## 一、我改的每一项

### 两条阻断

**1. ch16 毕业飞行分叉 —— 部分解决，改出了新问题**

先回答任务书的两个具体问题：

- 照 E1 基准答案作答的读者 → **能干净落进 W**。E1 基准的第 1 分钟（计数器、不动参数）与前 10 分钟
  （估量级、设观察指标）没有任何一项改变系统行为，`ch16.html:462-466` 的规则又明写「W 是 E1 基准答案
  推荐的做法」。这一半过。
- Opus 的答案 → **落不干净，按规则字面反而落 Y**。他的第 1 分钟 #2 是拆批（Z），前 10 分钟又写了
  「限流阈值设为 Q_guard = 2×Q2」（是不是「收紧入口」他自己都拿不准，见 V2-TRY-answer E2 开头）。
  规则第一句说取「最先真正改变系统行为的那一个动作」→ Z；第二句说「若你写了不止一项，按 X > Y > Z > W
  取影响最大者」→ Y。两句打架，Opus 恰好卡在打架处。**必须二选一**：要么只认「最先」，要么只认优先级。
  我建议只留优先级（后果由影响最大的动作决定更合理），并删掉「最先……不是最先写下的那一句」那半句。
  另：规则用词是「改变**系统**行为」，而 Z 的定义是「改到达过程」——拆批改的是输入不是系统，较真的读者
  会说拆批不算、于是掉进 W。措辞应改成「改变系统或它的输入」。

完备性与互斥性：

- **「其他」类没封口。** 一个在 E1 就写「给扣减成功/创建失败加自动补偿」「把服务端对渠道的重试调低」
  「关掉 realtime restock」的读者，四类都不属于，规则没给兜底。补一句「其余改动本题不模拟，按对流量形状
  影响最接近的一类归入；都不像就走 W」即可。
- 互斥性靠优先级规则基本成立，Y/Z 边界（「缩小本批数量」算收紧入口还是拆批）可辩但可接受。

新问题（都在 E2 分支现场里）：

- `ch16.html:489-490` Y 分支：「**被限住的那批请求**里有 37 条日志显示『扣减成功、订单创建失败』」——
  被网关限住的请求到不了库存扣减，不可能「扣减成功」。物理上自相矛盾。应为「放行的那批里仍有 37 条」。
- `ch16.html:505-509` W 分支：旁白说「你付出的代价是这一批没有被保护」，但日志里**没有一条坏消息**——
  库存不告警、缺口不扩大、连接池不排队。与 X 并排读说不通：同一个第 3 批，X 加了机器缺口涨到 23%，
  W 什么都没加却既没过载也没放大缝。要么给 W 一条真实代价（如「库存服务 P99 从 43ms 抬到 ~400ms、
  获取连接等待出现」——这还顺手兑现了 E1 基准的 No.01 线索），要么别声称有代价。现在 W 是无代价的
  最优解，「不同分支证据强度不等」这个教学点被「W 支配一切」盖过去了。
- `ch16.html:497` Z 分支把「缺口没有随之改善，仍在约 10%」标成 `WORSE`；同一事实在 W 里标 `STEADY`。
  标签语义不一致。
- `ch16.html:519` `.ex-hint` 残留「**两个分支**的正确诊断方向其实相通」——四分支改漏了这句。
- 基准里 Z 段说「缺口不随 QPS 变化，说明这条缝不是过载造成的」，而 X 段刚说缝被更大流量放大到 23%。
  W 段用「过载成分 + 固定成分」把两者接上了，但 Z 段的表述是绝对化的，应改成「这 10% 的固定成分不是
  过载造成的」。

**2. ch01 闭卷估算 —— 未完全解决，改出了新问题**

题面 / 参考解 / 自评现在**仍不是同一道题**：

- 题面 `ch01.html:101` 的 ⑦ 只说「本章暂不要求算机器数」，**没有把尖峰场景列为交付物**——尖峰只出现在
  textarea 的 placeholder 里（`ch01.html:102`「⑦ 尖峰场景（蹲点人数/窗口/人均请求 → QPS）」）。
  自评 `ch01.html:167` 却要求「算出了日常峰值 QPS 和尖峰场景 QPS，并且写出了三个额外假设」。
  题面文字里没有任何一条要求算 QPS（连日常峰值 QPS 都没要求），自评在考题面没出的东西。
- 自评最后一条「提出了具体的下一步验证动作（压测什么、怎么压）」——题面从头到尾没要这个交付物
  （这一条改前就错位，这次没动）。
- 参考解 `ch01.html:157` 仍有整节「从 QPS 到机器数：**别停在 QPS**」算出 30–38 台，`ch01.html:159`
  最敏感假设一节也在讲机器数区间怎么摆——与题面新加的「本章暂不要求算机器数」正面冲突。
  留作延伸可以，标题不能再叫「别停在 QPS」。
- **句子被改残**：`ch01.html:109` 现在是「按取值往下推：（翻场次列表、搜索、看详情、下单各若干次）；
  日常峰值倍数按 ×10（参考同类电商大促的经验区间 6–15 倍，取中间偏保守）：」——括号里那句本来修饰
  「30 次请求」，主语被删掉了；峰值倍数那句与上一句新写的内容重复。
- 「先给区间再取点值」只给了 DAU / 人均 / 峰值倍数三个，④ 响应大小 ⑤ RT 没给区间（表里仍是 300 KB、
  0.2s 点值）。题面说「五个数都要给上下界」，参考解只示范了三个。

### 事实错误

| 项 | 判定 | 说明 |
|---|---|---|
| ch16 No.08 惊群误引 | **已解决** | 与 ch07 No.08 卡的机理（`ch07.html:257` 起：统一超时/固定 backoff/统一 TTL 同步醒来）一致。但副作用见「新发现」#3：基准答案风险①现在没有卡号，而题面 ③ 要求「每个配一个病历卡编号」。 |
| ch03/07/12/15 页脚关卡标题 | **未完全解决** | 「下一章 →」方向的四处改对了；「← 上一章」方向漏了三处：`ch04.html:437`「← 实战关卡 1 · 数据库生死演练」、`ch08.html:449`「← 实战关卡 2 · 云服务级联演练」、`ch16.html:664`「← 实战关卡 4 · 多活与脑裂演练」，仍是编的（ch13 那处改了）。 |
| ch05「姊妹篇」 | **已解决** | `ch05.html:126` 改指 No.16 亚稳态。 |
| ch08 2 vs 3 | **已解决，改出了新问题** | 2.2 的算术对（0.3 + 0.7/5 = 0.44，÷0.2 = 2.2）。但 `ch08.html:280` 出现「两种模式的两种模式的最热分片/平均」重复词。 |
| ch01 连接池 20 → ch02 50 | **已解决** | `ch02.html:70`。 |
| ch12「全部登记」 | **已解决** | 逻辑通了；同一句里五道防线列了两遍，行文冗余，可再收一刀。 |

### D1 槽位置换 —— 分布对，语义对，但结构坏了

- 分布：good 落在 A=ch01/05/09/13、B=ch02/06/10/14、C=ch03/07/08/12、D=ch04/11/15/16，各 4 ✔。
- 11 章判词首句字母 ↔ 选项内容逐条核过，**判词正文与选项语义全部对得上**，没有「A 的问题是…」但 A 是正确项的情况。
- 但：① 11 章丢了标题和 h3（阻断 #1）；② `ch05.html:186` 正文引用错位（阻断 #2）；
  ③ 被置换的 11 章判词首句变成「A的问题是」无空格，未置换的 5 章仍是「A 的问题是」有空格；
  ④ 被置换的章判词按 A→D 顺序排，未置换的章「教科书答案」放第一段——现在两种排法并存。
  ③④是风格不一致，顺手修。

### 评分尺 —— 已解决，改出了新问题

两档改成可勾选行为 ✔。但新增的累积规则（`tools/derive.py:187` → `appendix-interview.html:97`）
「必须同时满足本档及所有更低档的行为才算达到该档」与「够用」档的**负向措辞**冲突：够用写的是
「无法从入口逐条边走到…」「没有为未选方案写出代价」「没检查数量级」——这些是缺陷描述，
不可能与「良好」的正向行为同时满足。规则按字面无法执行。要么把够用档改写成正向最低行为，
要么把规则改成「更低档的正向部分是本档前提」。derive 与产出一致（scratch 里重跑 derive.py，
appendix-interview / appendix-cards / derived-counts 三者零 diff）。

### 口径 —— 部分解决

- 五机制/四类：`index.html:53/55` 通了 ✔。
- 只在两个地方 → 三处 9 块：**未解决**。`index.html:86` 仍写「还有第五个机制，**它只在两个地方出现**」，
  紧接着 `index.html:105` 又说「三处、共 9 个练习块」。同一节自相矛盾。9 块本身数得对（1+2+6）。
- 两条 → 三条阅读路径 ✔（第三条是段落不是卡片，读者找得到）。
- 「全书唯一」×3：ch01/ch16 的 `.ex-warn` 改成了「这一页是这类练习专门检验你的独立输出」
  （`ch01.html:106` `ch16.html:438`）——**这句是病句**，「这一页是……检验你的独立输出」主谓不搭。
  `index.html:86`「全书唯一要求你从空白页产出的东西」保留是对的（说的是机制不是页面）。

---

## 二、9 条未改项的分类

**必须补（都便宜，一两句话，且读者可见）**

- **#8 E1 决策门槛无基线。** E2 的 X 分支已经写「从**约 10%** 扩大到约 23%」——E2 默认读者知道 10%，
  E1 却只说「少一截」。在 E1 GAP 那行加「粗看约一成」即可，难度不降（Opus 说得对：难点不是这个数）。
- **#9 核心症状没有病历卡。** 这条在 No.08 修掉之后升级了：基准答案三个风险里，①没有卡号、③的 No.03
  自己都承认套不上（E2 基准写「不能照抄」），而题面 ③ 仍要求「每个配一个病历卡编号」。**基准答案现在
  不满足自己的题面。** 最小修法是改题面：「配一个病历卡编号；如果 17 张卡里没有合适的，写『无卡』并
  自己给它命名——这也是考点」。新卡可以留下一版。
- **#6 关卡一承诺「第 16 章会把运气变成演练」。** `level1.html:240` 明写，ch16 全文没有「运气」二字，
  顺读读者会等一个不出现的回扣。在 16·5 或 16·6 加一句回收即可。
- **#1 45 分钟 vs mock 标时。** `ch16.html:615` 第 4 周写「45 分钟计时」，附录 B 三道 mock 都是 20+15=35，
  节奏表又是 45。两页数字互相打架，一句话就能平（把 ch16 改成 35，或在附录 B 说明 mock 只练前两段）。
- **#2 Mock 2/3 基准答案。** 我没有 Codex 的原文，按自己读的判断：Mock 3 阶段一是编排（事件链、
  下一个服务订阅上一步的事件）却又有「订单状态表」和阶段二的「自动重试队列」——编排式里没有中央
  重试队列可摘，两种模式混在一起没交代是哪种；Mock 2 阶段二把「小城市热点被淹没」归因于「聚合资源被
  一线城市挤占」，真正的产品问题是排序归一化（绝对计数 vs 城内相对热度），资源是次要的。基准答案
  是读者对照自评的锚，含架构级混淆不能上；每题一段话重写即可。至少 Mock 3 必须补。

**可留下一版**

- **#3 鉴别诊断表混坐标轴。** 是真的（「单点尖刺」组里三行看的是不同曲线），但加一列「哪条曲线」就够，不影响可用。
- **#4 三条路径衔接。** ch16 → 附录 B 有链（`ch16.html:658`），index 顺读主线也补了；缺的是附录 B 页内
  反向认账。体验问题，不是错误。
- **#5 ch16·3 十四张 case-ref 总检。** 结构性砍法，与本次修复无关，留到下一版。
- **#7 判词模板化。** 风格问题；但注意置换把 11 章判词排成 A→D 顺序之后，模板句式比以前**更显眼**了
  （连着四段「A的问题是 / B会让事情更糟 / C方向没错 / D是教科书答案」）。下一版一起处理。

**不该做**：无。9 条里没有一条是方向错的。

---

## 三、新发现

1. **Opus 报告里的两条「硬」异议既没改也没进「知道但没改」清单**（V2-TRY-REPORT §3）：
   - `ch16.html:525`「不能照抄**第 3 章**的处方」——No.03 在附录 A 和 `ch04.html:190` 里都归**第 4 章**；
     第 3 章是复制延迟（No.02）。这是把卡号当章号，读者真会去翻第 3 章。**必须改，一个字。**
   - `ch16.html:527` 基准监控「扣减成功时间戳 → 订单确认时间戳的差值分布」被声称「能同时回答分支 Y 的
     『限流有没有误伤』」——被 429 挡在门外的人**不产生扣减时间戳**，不在这个分布里。这次修复非但没改，
     还把同一条声明**扩到了 Z 和 W**。**必须改**：加一句「Y 分支还需要一条『被拒计数』，这条分布数不到门外的人」。
2. Opus 的另两条软异议也没被记录：E1 基准把「加计数器」标成「止血，不诊断」（`ch16.html:449`），
   而题面 ④ 恰恰考止血/诊断分类；以及「不许改参数、却默许临时加代码计数」的风险等级倒挂
   （现在 W 分支的「临时加的计数器」也建立在这上面）。可留下一版，但该进清单。
3. 上文已提：基准答案风险①无卡号 vs 题面 ③ 要求配卡（与 #9 合并处理）。
4. ch04/ch08/ch16 三处「← 上一章」关卡标题仍是编的（上文事实错误表）。
5. `index.html:86`「只在两个地方出现」漏改（上文口径）。
6. `ch08.html:280` 重复词；`ch01.html:109` 残句；`.ex-warn` 病句（上文）。
7. `SKELETON.md:11-12` 仍是「四机制 / 两条阅读路径」——V2-EDITOR-REPORT §1 第 3 条提过，未动。
   不影响读者，但下一批写作者会按旧契约干活。
8. `validate.py` 该加一条：`.decision` 内必须有 `.decision-tag` 与 `<h3>`。这次就是缺这条检查让 11 章
   静默丢标题（`validate_mutation_test.py:50` 的模板里其实有 decision-tag，只是 validate 没验）。

---

## 四、建议的最小修复顺序

1. 从 `d3b9617^` 取回 11 章 D1 的 `decision-tag` + `<h3>`（机械活，可脚本化，之后给 validate 加检查）。
2. `ch05.html:186` C → A。
3. ch16：分派规则二选一 + 「其他」类兜底 + Y 分支「被限住」措辞 + W 分支给出真实代价 + `ex-hint`「两个分支」
   + 「第 3 章」→「第 4 章」+ 监控在 Y 上盲的说明 + Z 段绝对化表述。
4. ch01：题面 ⑦ 正式写入尖峰交付物；修残句；「别停在 QPS」标题与题面对齐；自评「下一步验证」要么进题面要么删。
5. 三处回链关卡标题、`index.html:86`、`ch08.html:280`、`.ex-warn` 病句、评分尺累积规则措辞。
6. 必须补的五条未改项（#8 #9 #6 #1 #2）。

以上全部改完再复审一次；这次的验收命令组合（validate/css-audit/bible-check/derive）**没有一条能发现
本轮最大的问题**，下轮验收请加一次「每章 D1 标题存在」的检查，以及一次纯人工的 D1 通读。
