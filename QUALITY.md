# QUALITY.md · 质量红线（系列教训 + 本书特有协议）

## 一、批量生产质量衰减六条（AI 系统手册返工得来，逐条执行）

1. **攻击可运行示例，而不是散文**：审计火力集中在公式代入、数字推演、模拟器实验剧本这类
   "可被证伪"的内容上；散文错误好改，示例错误毁信任。
2. **绝对化声明必须挂旗**：全文 grep「永远/唯一/必然/绝不/所有/一定」，逐条要么补成立条件，
   要么改限定表述。
3. **校验器要做变异测试**：validate.py 每加一条规则，先人为制造一个该规则应当抓住的错误，
   确认能抓住，再信任它。
4. **全局计数一律程序派生**：病历卡总数、章节数、面试题总数、"全书 N 个模拟器"这类数字
   禁止手写，由脚本从文件树/DOM 统计生成或校验。
5. **判断型收录页必须人工复审**：附录图鉴的"按症状形状分组"、面试映射的问题挑选，
   属于判断题而非机械题，脚本过了也要通读复审。
6. **修一个错先 grep 它的所有变体**：发现某个错误表述时，搜索其同义变体（中英文、缩写、
   不同措辞）确认全部修净，再关闭该问题。

发布前追加**全站横切检查**：章间引用与骨架一致性、病历卡编号全书唯一且与附录一致、
导航链完整（每页可达上一/下一/目录）、深色模式逐页目检、移动端宽度目检。

## 二、真实事故事实核查协议（本书最高优先级红线）

四个实战关卡 + 病历卡案例栏涉及真实公司与真实损失，错误事实 = 全书信誉归零。

1. **原始来源强制**：每个关卡构建前，必须联网获取并通读官方原始复盘
   （GitLab postmortem 博文 / AWS 官方 Summary / SEC 对 Knight Capital 的行政令 /
   GitHub post-incident analysis；Facebook 2010 与 2021 的官方工程博文）。
   二手文章只能提供线索，不能作为事实依据。
2. **逐句核对**：关卡页面中每一个事实性陈述（时间、时长、金额、机制、决策）建立
   核对清单（sources/ 目录下每关卡一个 FACTS-levelN.md：陈述 → 原文出处段落）。
3. **数字一律带"约"**，除非原文给出精确值且直接引用语境明确。
4. **无法核实的细节：删除或改写为明确标注的"改编"**。关卡页头统一放置
   「根据官方公开复盘改编，细节经过叙事化处理」声明；改编只允许发生在氛围与心理描写，
   不允许发生在技术事实与决策链。
5. **不虚构引语**：不给真实人物编造台词；值班对话只出现在虚构的「秒抢」剧情里。
6. **不指名批评**：真实事故的教训表述对事不对人，与各家官方复盘的 blameless 精神一致。

## 三、模拟器验证协议

1. 每个模拟器先写 tools/simN-model-test.js（无头复制状态机核心），
   对正文实验清单的**每一条声称现象逐条断言**；测试通过才允许写正文声称。
2. 参数默认值写入测试（防止后续调参悄悄破坏教学剧本）。
3. 页面内联脚本与无头模型如有逻辑分叉，以页面为准同步回测试——两者漂移视为构建失败。
4. 每个模拟器手动目检：暂停/重置/极端滑块值/连点按钮不产生 NaN 或图表崩坏。

## 四、页面机械校验（tools/validate.py，对全部页面运行）

- id 全页唯一；标签配平；每个 data-d / data-q 有对应 verdict 目标
- 禁用词：localStorage、sessionStorage、http://、外链 CDN
- 每章七段式结构齐全（按 h2 的 sec 编号检查；无模拟器章节允许缺第 4 段）
- 每章自测恰好 4 题、面试映射恰好 3 问、教训恰好 3 条
- 病历卡结构五行齐全、编号唯一、症状行含形状符号
- 章间引用「第 N 章」的 N 必须在 1–16 且与骨架主题匹配（人工抽查配合）
- 绝对化词汇清单输出待复核列表（对应六条中的第 2 条）
